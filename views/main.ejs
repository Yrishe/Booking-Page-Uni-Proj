<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lounge</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center m-0">
    <!-- Top Section -->
    <header class="p-2">
        <div class="text-center space-y-2">
            <div class="bg-gray-300 text-black font-bold py-2 px-4 rounded-full inline-block">BuyTickets.com</div>
            <div class="bg-gray-300 text-black font-medium py-2 px-4 rounded-full inline-block">Lounge</div>
            <div class="bg-gray-300 text-black text-sm py-2 px-4 rounded-full inline-block">By: Yarli Rabelo</div>
        </div>
    </header>

    <main class="flex flex-col items-center justify-start gap-12 mt-2">
        <div class="bg-gray-100 flex items-center justify-center p-12">
            <div class="bg-white rounded-lg shadow-lg max-w-md w-full p-6">
                <h1 class="text-2xl font-bold text-gray-800 mb-4">Admin Portal</h1>
                <p class="text-gray-600 mb-6">
                    Sign in as an administrator to create, modify, delete events, add, and remove members.
                </p>
                
                <div class="bg-gray-50 p-4 rounded-lg shadow-inner mb-6">
                    <h2 class="text-lg font-semibold text-gray-700 mb-2">Admin Credentials:</h2>
                    <ul class="text-gray-600">
                        <li class="mb-2">
                            <span class="font-medium text-gray-800">Username:</span> <%= credentials.username %>
                        </li>
                        <li>
                            <span class="font-medium text-gray-800">Password:</span> <%= credentials.password %>
                        </li>
                    </ul>
                </div>
        
                <p class="text-gray-600">
                    <span class="font-semibold text-gray-800">Note:</span> Members can only access the platform if they are on the approved list.
                </p>
            </div>
        </div>
        
        <div class="bg-white shadow-lg rounded-lg p-8 w-96 border">
            <h2 class="text-2xl font-bold text-center mb-6">Organizer</h2>
            <form action="/users/sign-in" method="GET" class="space-y-4">

                <!-- Submit Button -->
                <div>
                    <button 
                        type="submit" 
                        class="w-full bg-blue-500 text-white py-3 rounded-full font-bold hover:bg-blue-600">
                        Sign In
                    </button>
                </div>

            </form>
        </div>

        <div class="bg-white shadow-lg rounded-lg p-8 w-96 border">
            <h2 class="text-2xl font-bold text-center mb-6">Visitor (Members)</h2>
            <form action="/users/sign-up" method="GET" class="space-y-4">

                <!-- Submit Button -->
                <div>
                    <button 
                        type="submit" 
                        class="w-full bg-blue-500 text-white py-3 rounded-full font-bold hover:bg-blue-600">
                        Check in
                    </button>
                </div>
            </form>
        </div>
    </main>
</body>
</html>
