<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmation - Library Booking Platform</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'library-blue': '#1e40af',
                        'library-green': '#059669',
                        'library-gray': '#6b7280'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <h1 class="text-xl font-bold text-library-blue">📚 Library Booking Platform</h1>
                </div>
                <nav class="flex items-center space-x-4">
                    <span class="text-sm text-gray-600">Welcome, <%= user.username %></span>
                    <a href="/users/logout" class="text-sm text-red-600 hover:text-red-800">Logout</a>
                </nav>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        <!-- Progress Bar -->
        <div class="mb-8">
            <div class="flex items-center justify-center">
                <div class="flex items-center text-sm">
                    <div class="flex items-center text-library-green">
                        <div class="w-8 h-8 bg-library-green text-white rounded-full flex items-center justify-center">
                            ✓
                        </div>
                        <span class="ml-2 font-medium">Event Selection</span>
                    </div>
                    <div class="w-16 h-0.5 bg-library-green mx-4"></div>
                    <div class="flex items-center text-library-green">
                        <div class="w-8 h-8 bg-library-green text-white rounded-full flex items-center justify-center">
                            ✓
                        </div>
                        <span class="ml-2 font-medium">Payment</span>
                    </div>
                    <div class="w-16 h-0.5 bg-library-green mx-4"></div>
                    <div class="flex items-center text-library-green">
                        <div class="w-8 h-8 bg-library-green text-white rounded-full flex items-center justify-center">
                            ✓
                        </div>
                        <span class="ml-2 font-medium">Confirmation</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Success Message -->
        <div class="text-center mb-8">
            <div class="w-16 h-16 bg-library-green text-white rounded-full flex items-center justify-center mx-auto mb-4">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
            </div>
            <h1 class="text-3xl font-bold text-gray-900 mb-2">Booking Confirmed!</h1>
            <p class="text-lg text-gray-600">Thank you for your booking. Your payment has been processed successfully.</p>
        </div>

        <!-- Booking Details -->
        <div class="bg-white rounded-lg shadow-md overflow-hidden mb-8">
            <div class="bg-library-blue text-white px-6 py-4">
                <h2 class="text-xl font-semibold">Booking Details</h2>
            </div>
            
            <div class="p-6">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <!-- Event Information -->
                    <div>
                        <h3 class="text-lg font-semibold text-gray-900 mb-4">Event Information</h3>
                        <div class="space-y-3">
                            <div>
                                <span class="text-sm font-medium text-gray-500">Event:</span>
                                <p class="text-gray-900"><%= booking.title %></p>
                            </div>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Description:</span>
                                <p class="text-gray-900"><%= booking.subtitle %></p>
                            </div>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Ticket Type:</span>
                                <p class="text-gray-900 capitalize"><%= booking.ticket_type %></p>
                            </div>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Quantity:</span>
                                <p class="text-gray-900"><%= booking.quantity %> ticket<%= booking.quantity > 1 ? 's' : '' %></p>
                            </div>
                        </div>
                    </div>

                    <!-- Booking Information -->
                    <div>
                        <h3 class="text-lg font-semibold text-gray-900 mb-4">Booking Information</h3>
                        <div class="space-y-3">
                            <div>
                                <span class="text-sm font-medium text-gray-500">Booking ID:</span>
                                <p class="text-gray-900 font-mono">#<%= booking.id %></p>
                            </div>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Booking Date:</span>
                                <p class="text-gray-900"><%= new Date(booking.booking_date).toLocaleDateString() %></p>
                            </div>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Status:</span>
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                    Confirmed
                                </span>
                            </div>
                            <% if (booking.invoice_number) { %>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Invoice Number:</span>
                                <p class="text-gray-900 font-mono"><%= booking.invoice_number %></p>
                            </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Payment Information -->
        <div class="bg-white rounded-lg shadow-md overflow-hidden mb-8">
            <div class="bg-gray-50 px-6 py-4 border-b">
                <h2 class="text-lg font-semibold text-gray-900">Payment Information</h2>
            </div>
            
            <div class="p-6">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <div class="space-y-3">
                            <div>
                                <span class="text-sm font-medium text-gray-500">Transaction ID:</span>
                                <p class="text-gray-900 font-mono"><%= booking.transaction_id %></p>
                            </div>
                            <div>
                                <span class="text-sm font-medium text-gray-500">Payment Method:</span>
                                <p class="text-gray-900 capitalize"><%= booking.payment_method.replace('_', ' ') %></p>
                            </div>
                        </div>
                    </div>
                    <div>
                        <div class="space-y-3">
                            <div>
                                <span class="text-sm font-medium text-gray-500">Amount Paid:</span>
                                <p class="text-2xl font-bold text-library-green">$<%= booking.total_amount.toFixed(2) %></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <% if (booking.invoice_number) { %>
            <a href="/products/invoice/<%= booking.id %>" 
               class="bg-library-blue text-white px-6 py-3 rounded-md font-medium hover:bg-blue-700 transition-colors text-center">
                📄 Download Invoice
            </a>
            <% } %>
            <a href="/products/customer-sales-page-d" 
               class="bg-gray-200 text-gray-800 px-6 py-3 rounded-md font-medium hover:bg-gray-300 transition-colors text-center">
                Browse More Events
            </a>
            <a href="/products/bookings" 
               class="bg-library-green text-white px-6 py-3 rounded-md font-medium hover:bg-green-700 transition-colors text-center">
                View My Bookings
            </a>
        </div>

        <!-- Important Information -->
        <div class="mt-8 bg-blue-50 border border-blue-200 rounded-md p-6">
            <div class="flex">
                <div class="text-blue-400">ℹ️</div>
                <div class="ml-3">
                    <h3 class="text-sm font-medium text-blue-800">Important Information</h3>
                    <div class="mt-2 text-sm text-blue-700">
                        <ul class="list-disc list-inside space-y-1">
                            <li>Please save this confirmation for your records</li>
                            <li>You will receive an email confirmation shortly</li>
                            <li>Present your booking ID at the event venue</li>
                            <li>For any questions, contact our support team</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t mt-12">
        <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
            <div class="text-center text-sm text-gray-500">
                <p>&copy; 2024 Library Booking Platform. All rights reserved.</p>
                <p class="mt-1">Need help? Contact us at support@librarybooking.com</p>
            </div>
        </div>
    </footer>
</body>
</html>