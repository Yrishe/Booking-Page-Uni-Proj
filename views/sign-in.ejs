<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In - EventHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'primary': '#6366f1',
                        'primary-dark': '#4f46e5',
                        'secondary': '#ec4899',
                        'accent': '#f59e0b',
                        'success': '#10b981',
                        'dark': '#1f2937'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Background Pattern -->
    <div class="absolute inset-0 opacity-5">
        <div class="text-6xl text-primary transform rotate-12 absolute top-10 left-10">🎫</div>
        <div class="text-4xl text-secondary transform -rotate-12 absolute top-20 right-20">🎪</div>
        <div class="text-5xl text-accent transform rotate-45 absolute bottom-20 left-20">🎭</div>
        <div class="text-3xl text-primary transform -rotate-45 absolute bottom-10 right-10">🎨</div>
        <div class="text-4xl text-secondary transform rotate-12 absolute top-1/2 left-1/4">🎵</div>
        <div class="text-3xl text-accent transform -rotate-12 absolute top-1/3 right-1/3">🎬</div>
    </div>

    <!-- Header -->
    <header class="relative z-10 p-6">
        <div class="text-center">
            <a href="/" class="inline-flex items-center text-primary hover:text-primary-dark transition-colors font-medium">
                <span class="mr-2">←</span> Back to Home
            </a>
        </div>
    </header>

    <!-- Main Content -->
    <main class="relative z-10 flex items-center justify-center min-h-screen px-4 -mt-20">
        <div class="bg-white shadow-2xl rounded-2xl p-8 w-full max-w-md border border-gray-200">
            <!-- Logo and Title -->
            <div class="text-center mb-8">
                <div class="text-4xl mb-4">🎫</div>
                <h1 class="text-2xl font-bold text-primary mb-2">Welcome Back</h1>
                <p class="text-gray-600">Sign in to your EventHub account</p>
            </div>

            <!-- Sign In Form -->
            <form action="/users/loggedIn" method="POST" class="space-y-6">
                <!-- Username Field -->
                <div>
                    <label for="username" class="block text-sm font-medium text-gray-700 mb-2">
                        👤 Username
                    </label>
                    <input 
                        type="text" 
                        id="username" 
                        name="username" 
                        required 
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary transition-colors"
                        placeholder="Enter your username"
                    >
                </div>

                <!-- Password Field -->
                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-2">
                        🔒 Password
                    </label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required 
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary transition-colors"
                        placeholder="Enter your password"
                    >
                </div>

                <!-- Submit Button -->
                <button 
                    type="submit" 
                    class="w-full bg-primary text-white py-3 px-4 rounded-lg hover:bg-primary-dark focus:ring-4 focus:ring-purple-200 transition-all duration-200 font-medium"
                >
                    Sign In to EventHub
                </button>
            </form>

            <!-- Error Message -->
            <% if (typeof error !== 'undefined' && error) { %>
                <div class="mt-4 p-3 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-red-600 text-sm text-center">⚠️ <%= error %></p>
                </div>
            <% } %>

            <!-- Demo Credentials -->
            <div class="mt-8 p-4 bg-gray-50 rounded-lg">
                <h3 class="text-sm font-medium text-gray-700 mb-3 text-center">🔑 Demo Accounts</h3>
                <div class="space-y-2 text-xs text-gray-600">
                    <div class="flex justify-between">
                        <span class="font-medium">Admin:</span>
                        <span>admin / admin123</span>
                    </div>
                    <div class="flex justify-between">
                        <span class="font-medium">Visitor:</span>
                        <span>visitor / visit123</span>
                    </div>
                </div>
            </div>

            <!-- Sign Up Link -->
            <div class="mt-6 text-center">
                <p class="text-gray-600 text-sm">
                    Don't have an account? 
                    <a href="/users/sign-up" class="text-primary hover:text-secondary font-medium transition-colors">
                        Sign up here
                    </a>
                </p>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="relative z-10 text-center py-6">
        <p class="text-gray-500 text-sm">&copy; 2024 EventHub. All rights reserved.</p>
    </footer>
</body>
</html>
